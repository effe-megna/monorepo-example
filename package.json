{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "clean": "rimraf packages/**/lib"
  },
  "devDependencies": {
    "@types/node": "^13.5.2",
    "lerna": "^3.20.2",
    "rimraf": "^3.0.1",
    "typescript": "^3.7.5"
  },
  "workspaces": [
    "packages/*"
  ]
}
